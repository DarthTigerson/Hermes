name: "🤖🧪E2E Testing"
on:
  pull_request:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run Cypress
        uses: cypress-io/github-action@v6